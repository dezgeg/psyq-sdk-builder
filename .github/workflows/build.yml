on: [push]
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - run: sudo apt-get install -y 7zip lhasa iat
            - uses: actions/checkout@v4
            - uses: actions/cache@v3
              with:
                  path: dl
            - run: make tarballs
            - uses: actions/upload-artifact@v4
              with:
                  path: tarballs/*.tar.gz
